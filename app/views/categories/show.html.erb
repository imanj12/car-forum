<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1><%= @category.title %></h1>
    <ul>
      <% @category.discussions.each do |dis| %>
        <li><%= link_to dis.title, discussion_path(dis) %></li>
        <% end %>
    </ul>

    <%= link_to 'Create new discussion', new_discussion_path %><br>

    <%= link_to 'See all Categories', categories_path %>

  </body>
</html>
